[package]
name = "cvcomposer"
version = "1.0.0"
description = "CVComposer - ATS CV Builder desktop app"
authors = ["you"]
edition = "2021"

[lib]
name = "cvcomposer_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
